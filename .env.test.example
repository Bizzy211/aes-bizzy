# =============================================================================
# A.E.S Bizzy - E2E Test Environment Configuration
# =============================================================================
# Copy this file to .env.test and fill in your credentials.
# NEVER commit .env.test to version control!
#
# Usage:
#   cp .env.test.example .env.test
#   # Edit .env.test with your credentials
#   docker-compose -f docker-compose.test.yml --env-file .env.test run test-runner --smoke
#
# VERIFIED: API keys sourced from .claude/mcp.json (2024-12-24)
# =============================================================================

# -----------------------------------------------------------------------------
# Test Configuration
# -----------------------------------------------------------------------------
TEST_MODE=smoke
LOG_LEVEL=info
TEST_TIMEOUT=300000

# -----------------------------------------------------------------------------
# AI Provider API Keys
# ANTHROPIC_API_KEY is REQUIRED for smoke/integration tests
# These are inherited by task-master-ai MCP server from environment
# -----------------------------------------------------------------------------

# Anthropic (REQUIRED for AI features and TaskMaster)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# OpenAI (Optional - for GPT-based operations)
OPENAI_API_KEY=sk-xxxxx

# Perplexity (Optional - for TaskMaster research features)
PERPLEXITY_API_KEY=pplx-xxxxx

# -----------------------------------------------------------------------------
# GitHub Integration
# REQUIRED for GitHub-related tests and repo sync
# Create a personal access token at: https://github.com/settings/tokens
# Scopes needed: repo, read:org
# -----------------------------------------------------------------------------
GITHUB_TOKEN=ghp_xxxxx

# Test repository (optional - for safe testing)
TEST_GITHUB_OWNER=your-username
TEST_GITHUB_REPO=test-repo

# -----------------------------------------------------------------------------
# Supabase Integration
# Required for projectmgr-context and supabase-2 MCP servers
# Get these from your Supabase project settings
#
# NOTE: Different MCP servers use different variable names:
#   - projectmgr-context: SUPABASE_URL + SUPABASE_KEY
#   - supabase-2: SUPABASE_API_KEY
#   - Official Supabase MCP: SUPABASE_ACCESS_TOKEN
# -----------------------------------------------------------------------------
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=eyJxxxxx
SUPABASE_ANON_KEY=eyJxxxxx
SUPABASE_ACCESS_TOKEN=sbp_xxxxx
SUPABASE_SERVICE_ROLE_KEY=eyJxxxxx

# -----------------------------------------------------------------------------
# Heimdall / Qdrant
# Usually auto-configured when using docker-compose
# Only set manually if using external Qdrant instance
# -----------------------------------------------------------------------------
# QDRANT_URL=http://localhost:6333

# -----------------------------------------------------------------------------
# MCP Server API Keys (VERIFIED from .claude/mcp.json)
# These are REQUIRED for their respective MCP servers to function
# -----------------------------------------------------------------------------

# Exa AI (REQUIRED for exa MCP - web search and code context)
# Get from: https://exa.ai/dashboard
EXA_API_KEY=xxxxx

# Ref.tools (REQUIRED for ref MCP - documentation search)
# Get from: https://ref.tools
# Format: ref-xxxxx
REF_API_KEY=ref-xxxxx

# Tavily (REQUIRED for tavily-search-server MCP - web search)
# Get from: https://tavily.com
# Format: tvly-xxxxx
TAVILY_API_KEY=tvly-xxxxx

# Firecrawl (REQUIRED for firecrawl-mcp-server - web scraping)
# Get from: https://firecrawl.dev
# Format: fc-xxxxx
FIRECRAWL_API_KEY=fc-xxxxx

# 21st.dev Magic UX/UI (REQUIRED for Magic UX/UI MCP - component generation)
# Get from: https://21st.dev
# CRITICAL: MCP config uses API_KEY (NOT MAGIC_21ST_API_KEY) - see .claude/mcp.json:119
API_KEY=xxxxx

# =============================================================================
# NOT USED - Previously documented but NOT in .claude/mcp.json:
# - CONTEXT7_API_KEY: No Context7 MCP server configured
# =============================================================================

# =============================================================================
# Security Notes:
# - Use test/sandbox accounts when possible
# - Create limited-scope tokens for testing
# - Consider using GitHub's fine-grained personal access tokens
# - For Supabase, consider using a test project
# - API keys in this file are placeholders - replace with real values
# =============================================================================
